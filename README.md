# Чтобы запустить проект кроме Cmake и gcc нужно установить:
-- SQlite
-- npm install

# Структура проекта:

├── .vscode/                 # Настройки VS Code
│   └── launch.json          # Конфигурации для отладки
│
├── client/                  # Клиентская часть приложения
│   ├── index.html           # Главная HTML-страница
│   ├── script.js            # Клиентский JavaScript
│   └── style.css            # Стили CSS
│
├── node_modules/            # Зависимости NPM
│
├── quze_core/               # Нативный модуль на C++
│   ├── build/               # Собранные бинарные файлы
│   ├── lib/
│   │   └── index.js         # JS-интерфейс для нативного модуля
│   ├── node_modules/        # Зависимости модуля
│   ├── src/
│   │   ├── quze_core.cpp    # Основной C++ код модуля
│   ├── test/
│   │   └── test-server.js   # Тесты для модуля
│   ├── CMakeLists.txt       # Конфигурация сборки CMake
│   ├── package.json         # Метаданные и скрипты модуля
│   └── package-lock.json
│
├── .gitignore
├── package.json             # Основные зависимости и скрипты
├── package-lock.json
├── questions.json           # Данные вопросов для игры
├── README.md                # Документация
└── server.js                # Сервер Node.js

# Структура базы данных:

Справочник категорий category:
  -id, тип целочисленной, автоинкремент 
  -name, тип строковый, уникальный


Справочник уровней сложности level:
  -id, тип целочисленной, автоинкремент 
  -name, тип строковый, уникальный

Создай таблицу вопросов questions:
  -id, тип целочисленной, автоинкремент 
  -text, тип строковый, уникальный  (текст вопроса)
  -A, тип строковый не пустой (вариант ответа A)
  -B, тип строковый не пустой (вариант ответа B)
  -C, тип строковый не пустой (вариант ответа C)
  -D, тип строковый не пустой (вариант ответа D)
  -answer, строковый не пустой (правильный вариант ответа)
  -id_category, тип целочисленной указатель на category.id
  -id_level, тип целочисленной указатель на level.id



Назначение ключевых файлов:
 -server.js: Основной сервер Express, обрабатывает API-запросы и интегрируется с quze_core
 -quze_core/src/quze_core.cpp: Ядро на C++ для высокопроизводительных операций
 -quze_core/lib/index.js: JavaScript-обёртка для нативного модуля
 -CMakeLists.txt: Конфигурация сборки C++ модуля
 -package.json:

- Node.js 18: NODE_MODULE_VERSION=108
- Node.js 20: NODE_MODULE_VERSION=115
- Node.js 21: NODE_MODULE_VERSION=120
- Node.js 22: NODE_MODULE_VERSION=127

# Полезные команды:

# -- Какая версия node используется по умолчанию?
which node - путь к программе
node -v

# -- Какая версия npm используется в проекте?
cd quze_core
npm run env | grep npm

# -- Просмотр локальных пакетов
npm list --depth=0 - Только пакеты верхнего уровня
npm list -g --depth=0 - Глобальные пакеты (сокращённо)

# -- Зависимости пакета
npm info <пакет> - Общая информация
npm view <пакет> dependencies - Только зависимости

# -- Удаление
npm un <пакет> - Локальный пакет (--save/--save-dev для зависимостей)
npm un -g <пакет> - Глобальный пакет

# -- Дополнительно
npm i <пакет> - Установка
npm outdated - Проверка устаревших версий
npm cache clean --force - Очистка кэша


# Описание протокола

%% Получение информации о категориях
Клиент->>Сервер: GET /api/info
Сервер-->>Клиент: {category: [{name: "...", count}]}

%% Начало игры
Клиент->>Сервер: POST /api/start {category: [...]}
 Сервер-->>Клиент: {} (200 OK)

loop Игровой цикл
%% Получение вопроса
Клиент->>Сервер: GET /api/getQuestion {}
Сервер-->>Клиент: 
    {
        question: {
            text: "...", 
            A:..., 
            B:..., 
            C:..., 
            D:...
        }
        level:10,
        help5050: true,
        helpAudience: true,
        helpLife: true,
    }


%% Отправка ответа
Клиент->>Сервер: POST /api/anwer 
{
    answer: "A"
}
Сервер-->>Клиент: 
{
   answer: true
}

%% Подсказки
Клиент->>Сервер: POST /api/help 
{
    type: "5050"   //'5050', 'life', 'audience'
}
Сервер-->>Клиент:
{ 
    question_enable: {   //Оставить активные кнопки ответа 
        A:true 
        B:false, 
        C:true, 
        D:false
    },
    question_postfix: {  //Дополнить дополнительным текстом в варианте ответа
        A:'(50%)',
        B:'(5%)', 
        C:'(30%)',
        D:'(15%)',
    }
}
